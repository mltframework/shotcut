/*!
    \qmlmodule org.snapflow.qml
    \title org.snapflow.qml QML Module
    \ingroup qmlmodules
    \brief Types for filter scripting, metadata, and file access in Snapflow.

    \code
    import org.snapflow.qml
    \endcode

    This module contains the core types used when writing Snapflow filter UI panels
    and VUI overlays. The most important type is \l Filter, which gives read/write
    access to an MLT filter's properties and keyframes.

    \section1 Types

    \annotatedlist qmltypes
*/
