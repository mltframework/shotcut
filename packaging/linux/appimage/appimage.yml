app: Snapflow
verbose: true

#ingredients:
#  dist: focal
#  sources:
#    - deb http://us.archive.ubuntu.com/ubuntu/ focal main universe
#  script:
#    - echo "multiarch-support" >> excludedeblist
                                                                                                                                         
script:
  - tar -xJ -C usr/bin --strip=1 -f ../../../../scripts/snapflow-linux-x86_64-*.txz
  - cp -p ../../../../icons/snapflow-logo-64.png snapflow.png
  - cp -p ../../../../icons/snapflow-logo-64.png org.snapflow.Snapflow.png
  - cp -p ../../../../packaging/linux/org.snapflow.Snapflow.desktop snapflow.desktop
  - desktop-file-edit --set-key=Exec --set-value=snapflow snapflow.desktop
